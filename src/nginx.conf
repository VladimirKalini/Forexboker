server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html index.htm;

  # Эта часть важна, чтобы роутинг в React работал
  location / {
    try_files $uri $uri/ /index.html;
  }
}
